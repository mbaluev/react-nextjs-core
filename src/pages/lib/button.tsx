import {Master} from '@/ui/system/master';
import {ButtonControls} from '@/ui/system/pages/lib/sections/ButtonControls';

const Button = () => {
  return <ButtonControls />;
};

Button.Layout = Master;
export default Button;
