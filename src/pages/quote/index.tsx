import {PageLayout} from '@components/pageLayout';
import {Master} from '@ui/shop/master';

const QuoteList = () => {
  return <PageLayout title="Quote List">Quote List</PageLayout>;
};

QuoteList.Layout = Master;
export default QuoteList;
