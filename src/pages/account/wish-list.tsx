import {PageLayout} from '@components/pageLayout';
import {Master} from '@ui/shop/master';

const WishList = () => {
  return <PageLayout title="Wish List">Wish List</PageLayout>;
};

WishList.Layout = Master;
export default WishList;
