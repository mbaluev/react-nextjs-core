import {PageLayout} from '@components/pageLayout';
import {Master} from '@/ui/shop/master';

const Profile = () => {
  return <PageLayout title="Profile">Profile</PageLayout>;
};

Profile.Layout = Master;
export default Profile;
